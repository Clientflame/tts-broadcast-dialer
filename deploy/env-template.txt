# ============================================================
# TTS Broadcast Dialer — Client Configuration
# ============================================================
# Copy this file to .env and fill in your values:
#   cp env-template.txt .env
# ============================================================

# --- App Branding ---
VITE_APP_TITLE="AI TTS Broadcast Dialer"
VITE_APP_LOGO=""
# APP_PORT=80

# --- White-Label Colors (OKLCH or hex format) ---
# Customize the color theme per client. Leave empty for default blue.
# OKLCH examples:
#   Blue (default):  oklch(0.55 0.15 240)
#   Green:           oklch(0.55 0.20 145)
#   Red/Orange:      oklch(0.55 0.22 30)
#   Purple:          oklch(0.50 0.20 280)
#   Teal:            oklch(0.55 0.15 195)
#   Gold:            oklch(0.60 0.18 85)
# Hex examples: #2563eb (blue), #16a34a (green), #dc2626 (red), #7c3aed (purple)
VITE_PRIMARY_COLOR=
VITE_ACCENT_COLOR=

# --- Database (auto-configured by docker-compose) ---
MYSQL_ROOT_PASSWORD=CHANGE_ME_root_password_here
MYSQL_DATABASE=tts_dialer
MYSQL_USER=dialer
MYSQL_PASSWORD=CHANGE_ME_db_password_here
# DATABASE_URL is auto-built by docker-compose from the above values

# --- FreePBX Connection ---
FREEPBX_HOST=your-freepbx-ip-or-hostname
FREEPBX_AMI_USER=admin
FREEPBX_AMI_PASSWORD=your-ami-password
FREEPBX_AMI_PORT=5038
FREEPBX_SSH_USER=root
FREEPBX_SSH_PASSWORD=your-ssh-password

# --- TTS API Keys ---
# At least one is required for TTS functionality
GOOGLE_TTS_API_KEY=your-google-tts-api-key
OPENAI_API_KEY=your-openai-api-key

# --- Authentication ---
# Generate with: openssl rand -hex 32
JWT_SECRET=CHANGE_ME_generate_a_random_secret

# --- Manus Platform (leave empty for standalone deployments) ---
VITE_APP_ID=
OAUTH_SERVER_URL=
VITE_OAUTH_PORTAL_URL=
OWNER_OPEN_ID=
OWNER_NAME=
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=
VITE_FRONTEND_FORGE_API_KEY=
VITE_FRONTEND_FORGE_API_URL=

# --- Timezone ---
TZ=America/New_York

# --- Auto-Update (Watchtower) ---
# How often to check for updates in seconds (default: 86400 = 24 hours)
UPDATE_CHECK_INTERVAL=86400

# --- Optional: Skip DB migrations on startup ---
# SKIP_MIGRATIONS=true
